services:
  flux-filters:
    image: ghcr.io/aut0nate/flux-filters:${IMAGE_TAG:-latest}
    container_name: flux-filters
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - flux-filters-data:/data
    networks:
      - edge-net
    expose:
      - "3000"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    logging:
      options:
        max-size: 10m
        max-file: "3"

networks:
  edge-net:
    external: true

volumes:
  flux-filters-data:
